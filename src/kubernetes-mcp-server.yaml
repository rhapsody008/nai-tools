---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kubernetes-mcp-server
  namespace: zy-gpu-workspace
spec:
  interval: 2m
  path: ../kubernetes-mcp
  prune: true
  sourceRef:
    kind: GitRepository
    name: zy-tools