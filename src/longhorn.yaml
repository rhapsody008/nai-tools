apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn
  namespace: zy-gpu-workspace
spec:
  interval: 2m
  path: ../longhorn
  prune: true
  sourceRef:
    kind: GitRepository
    name: zy-tools