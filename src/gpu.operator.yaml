apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gpu-operator
  namespace: zy-gpu-workspace
spec:
  interval: 2m
  path: ../gpu-operator
  prune: true
  sourceRef:
    kind: GitRepository
    name: zy-tools